pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building in progress'
            }
        }
        stage('Dockerize') {
            steps {
                echo 'Generating docker image'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy inprogress'
            }
        }
    }
}
